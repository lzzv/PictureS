<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="baidu.com.pictures.MainActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#fff"
        android:gravity="center"
        android:padding="12dp"
        android:text="壁纸精选"
        android:textColor="#000"
        android:textSize="20sp"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="#6f000000"
        />

    <FrameLayout
        android:id="@+id/fl_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        >

        <RadioGroup
            android:id="@+id/rg_tab"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <RadioButton
                android:id="@+id/rb_recommand"
                style="@style/BottomTabStyle"
                android:drawableTop="@drawable/selecter_recommand"
                android:text="推荐"/>

            <RadioButton
                android:id="@+id/rb_classify"
                style="@style/BottomTabStyle"
                android:drawableTop="@drawable/selecter_classify"
                android:text="分类"/>

            <RadioButton
                android:id="@+id/rb_search"
                style="@style/BottomTabStyle"
                android:drawableTop="@drawable/selecter_search"
                android:text="搜索"/>

            <RadioButton
                android:id="@+id/rb_more"
                style="@style/BottomTabStyle"
                android:drawableTop="@drawable/selecter_more"
                android:text="更多"/>

        </RadioGroup>

    </LinearLayout>

</LinearLayout>
